function sum(num1: number, num2: number) {
  return num1 + num2;
}

console.log(sum(1, 3));
const hello = "hello world";
console.log(hello.toUpperCase());

const user = {
  name: "Yan Forni de Medeiros",
  email: "yanfornimedeiros@gmail.com",
};

console.log(user.name);
