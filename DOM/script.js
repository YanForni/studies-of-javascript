//pegar elementos pela classe
const element = document.getElementsByClassName('one')
console.log(element);

// //pegar elementos pela tag
const elementTag = document.getElementsByTagName('meta')
console.log(elementTag);

//querySelector
const elementOne = document.querySelector('.one')

//usar preferencialmente, pois funciona algumas funções a mais 
//querySelectorAll
const elementAll = document.querySelectorAll('.one')